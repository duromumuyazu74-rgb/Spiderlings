# Runtime ownership

The manifest owns script loading order. Runtime scripts remain plain JavaScript in KD's native global environment, sharing the `Spiderlings` namespace. This structure applies to the `0.92.36-test.40` development package.

| Module                                                      | Responsibility and interface                                                                                                                                                                               |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SpiderlingsCore.js`                                        | Shared named registration, restraint catalog, text registration, summon-message compatibility, and owned hook composition.                                                                                 |
| `SpiderlingsEncounters.js`                                  | Configuration, population caps, Mage placement before native population, the unchanged squad, nest reinforcement and encounter events. `EncounterRules` and `ReinforcementRules` remain available.         |
| `SpiderlingsWebCaster.js`                                   | Qualification for crossfire and WebCaster movement preferences inside native movement budgets.                                                                                                             |
| `SpiderlingsModelRuntime.js`, `SpiderlingsWebbingModels.js` | Texture readiness and model registration.                                                                                                                                                                  |
| `SpiderlingsWebbingData.js`                                 | Immutable Webbing definitions, family metadata, messages and constants shared by rules and native integration.                                                                                             |
| `SpiderlingsWebbingRules.js`                                | `resolveWebbingAction({catalog, snapshot, action})` computes outcomes without native game globals. Its catalog and classification helpers serve the KD adapter.                                            |
| `SpiderlingsWebbing.js`                                     | Reads native equipment and eligibility, applies rule outcomes, registers restraints/events, and handles native escape and UI integration. The existing `Spiderlings.Webbing` caller interface is retained. |
| `SpiderlingsCombat.js`, `SpiderlingsJumperDash.js`          | Owned hit resolution and Jumper action lifecycle.                                                                                                                                                          |
| `SpiderlingsMage.js`                                        | Mage projectile collision with hostile Maidforce and native NPC damage. Player hits use the bolt's native `Damage` effect.                                                                                 |
| `SpiderlingsMageRunes.js`                                   | Chooses rune casts within the native Mage spell action; saves placement, trigger and delayed blast phases on a single rune bullet; resolves the 3-by-3 silk effect after one warning turn.                 |
| `SpiderlingsMageVisuals.js`                                 | Filters the supplied blue matte at draw time and draws particles with rune or projectile glow after a successful native Mage cast; visual timers are not saved.                                            |
| `SpiderlingsMageSpells.js`                                  | Runs the Mage's 4-by-4 shield hex and 21-cell inward silk collapse, including marks, shield fragility, delayed bursts, cooldown, and existing Normal/Pink web art.                                         |
| `SpiderlingsInfestation.js`                                 | Infestation objectives, depth-weighted five-nest grouping, idle dispersal patrols and exit conditions.                                                                                                     |
| `SpiderlingsSpinnerTopology.js`                             | JSON-only lines, declared orthogonal enclosures, normalized shared durability, composite-layer scheduling, reachability queries and final-owner collapse rules.                                            |
| `SpiderlingsSpinnerNativeField.js`                          | Projects topology cells into attackable KD entities, validates paid movement and work, routes final native damage, reconciles saves and refreshes navigation caches.                                       |
| `SpiderlingsSpinnerAI.js`                                   | Forms saved hostile Spinner groups, plans target-independent fields, assigns one stable lure after native perception, ages shared last-known data and schedules one paid action per actor pass.            |
| `SpiderlingsSpinnerScenarios.js`                            | Supplies ten bounded native scene fixtures, exact actor/target selection, saved control rehydration and ownership-preserving teardown. It does not implement an alternate simulation.                      |
| `SpiderlingsSpinnerRollout.js`                              | Snapshots generation eligibility and persists an autonomous enclosure for every eligible group; authored line fixtures remain supported, without a global field or enclosure cap.                          |
| `SpiderlingsSpinnerCapture.js`                              | Native composite-field admission, temporary Capture strands, contest/wrap actions and native input. Rendering consumes a read-only capture view.                                                           |
| `SpiderlingsSpinnerRecoveryCore.js`                         | Item-independent source audit, executor choice, destination arbitration and paid Spiderlings-web crossing shared by player and NPC recovery.                                                               |
| `SpiderlingsSpinnerNPCCapture.js`                           | Admits hostile NPCs after a native Spinner Slime hit, pins voluntary movement, observes native struggle and applies one aggregated native binding contribution per turn.                                   |
| `SpiderlingsSpinnerRecovery.js`                             | Owns real-leash recovery sources, destination arbitration, stand-firm/source-removal actions, and paid crossing of built Spiderlings web cells.                                                            |
| `SpiderlingsSpinnerNPCRecovery.js`                          | Records breached NPC departures and item-free recovery sources, then adapts shared paid pulling to native `KDMoveEntity`.                                                                                  |
| `SpiderlingsSpinnerField.js`, `SpiderlingsSpinnerArt.js`    | Legacy training-room geometry/construction compatibility and character artwork.                                                                                                                            |
| `SpiderlingsSpinnerRuntime.js`                              | Owns the single Spinner enemy-loop wrapper, composes native `hunt` perception/action gates and dispatches field damage, movement, load and positive-turn events.                                           |

Core precedes encounters and WebCaster movement. Webbing data precedes Webbing rules, which precede the native Webbing adapter. Tests explicitly load these dependencies; the independent manifest allowlist test protects delivery order. Rule-only tests load data and rules without registering enemies, items, native hooks or game events.

## Shared native hooks

`Spiderlings.Hooks.wrap(owner, native, create)` preserves the supplied native function and returns a stable wrapper. Reinstalling an owner already present in that Spiderlings chain replaces its callback at the existing position. This keeps new callbacks aligned with newly registered events without accumulating wrappers. `describe(handler)` reports owners in installation order.

| Native function              | Installation order, inner to outer                    | Delegation                                                                                                                    |
| ---------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `KDAIType.hunt.beforemove`   | `WebCaster.hunt`, `Cocoon.hunt`, `Spinner.beforemove` | Spinner coordination runs after native sensing and after the existing owned movement hooks decline the actor.                 |
| `KDAIType.hunt.attack`       | `Spinner.attack`                                      | Field, lure, yield and wait decisions suppress the later native attack phase. Defense and delegated pursuit retain it.        |
| `KDAIType.hunt.spell`        | `Spinner.spell`                                       | Uses the same per-actor, per-world-turn decision as the attack gate.                                                          |
| `KDAIType.wander.beforemove` | `Cocoon.wander`                                       | Unrelated calls reach the original handler.                                                                                   |
| `KinkyDungeonEnemyLoop`      | `Spinner.runtime`                                     | Native field construction, legacy training and capture are ordered delegates; every unhandled call reaches the original loop. |

Each callback forwards the original receiver, arguments and result on its unhandled route. The registry tracks Spiderlings-owned wrappers only. It does not inspect or replace another Mod's private wrapper chain, and it is not a general-purpose Mod hot-reload system. Other compatibility hooks retain their existing owners and guards.

## State ownership and rendering

| State                                                                                               | Owner and persistence                                                                          |
| --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Capture phase, target cell, admitted composite ID, participant IDs, contest work and active bag ID  | `KDGameData.SpiderlingsSpinnerCapture`; schema 3 is audited through `afterLoadGame`.           |
| NPC Capture target, admitted composite, participant IDs, strand debt and completion threshold       | `KDGameData.SpiderlingsSpinnerNPCCaptures`; schema 1 is audited without a free load tick.      |
| Recovery carrier ID, source records, executor, resistance, removal work and pending web crossing    | `KDGameData.SpiderlingsSpinnerRecovery`; schema 2 migrates schema 1 and audits current IDs.    |
| NPC breached departures, target/source IDs, executor and pending web crossing                       | `KDGameData.SpiderlingsSpinnerNPCRecoveries`; schema 1 contains no item or entity objects.     |
| Six-turn post-escape participant hold                                                               | `entity.SpiderlingsSpinnerStunTurns`; decremented on later positive world turns.               |
| Field phase, traps, connections and builder IDs                                                     | `KDMapData.SpiderlingsSpinnerField`.                                                           |
| Native physical graph, declared fields, line metadata, composite layers, HP, cooldowns and age      | `KDMapData.SpiderlingsSpinnerEncounter`; plain JSON saved with the active map.                 |
| Spinner group IDs, members, plan, assignments, stable target/lure, last-known age and action counts | `KDMapData.SpiderlingsSpinnerEncounter.ai`; plain JSON saved with the active map.              |
| Rollout enable decision and deterministic enclosure choice                                          | `KDMapData.SpiderlingsSpinnerRollout`; written once at map generation and reused on revisit.   |
| Infestation objective IDs, requested and actual nest groups, quiet timer and patrol ordinal         | `KDMapData.SpiderlingsInfestation`; existing maps retain their nest positions.                 |
| Infestation patrol goal and last native movement progress                                           | `entity.SpiderlingsInfestationPatrol`; native AI remains responsible for movement and combat.  |
| One-time Mage map-start outcome                                                                     | `KDMapData.SpiderlingsGuaranteedMageState`; written before native random population.           |
| Mage hex fields, marks, warning blasts and collapse charges                                         | `KDMapData.SpiderlingsMageSpells`; plain map-local state advanced on positive world turns.     |
| Mage collapse cooldown                                                                              | `entity.SpiderlingsCollapseCooldown`; decremented on later positive world turns.               |
| Debug scene actor/target inputs, prior encounter and actor state                                    | `KDGameData.SpiderlingsSpinnerScenarioControl`; restored after load and consumed by teardown.  |
| Native web-cell entities                                                                            | Reconciled projection of the encounter topology; exactly one owned proxy per solid cell.       |
| Deposited silk and leg-bag escape work                                                              | The equipped item's `data`, including `wrapProgress` and `SpiderlingsLegbinderEscapeProgress`. |
| Graphics, texture promises, animation interpolation and timer handles                               | Runtime-only values; not serialized.                                                           |

Capture admission is inspected without changing saved state. Native tick, movement, enemy and load events commit participant changes or interruption. Drawing can hide invalid previews, but cannot cancel capture, create retry timers, migrate fields or advance work. A lost contest creates no equipment. Each paid wrapping turn deposits 0.2 into the exact equipped item, and a source audit ends temporary control at zero sources without changing that item. The load handler audits schema-3 sources and the saved bag ID, reads deposited progress from the item and clears older capture state instead of inferring work. `phase()` and `isControllingPlayer()` expose read-only control state so recovery can pause without sharing source or leash ownership.

Recovery records at most eight distinct successful hitters. Audits remove dead, nonhostile, incapacitated, distant or occluded sources and never reconnect them without another hit. One stable executor pays each pull operation. A shared composite uses its common core; unrelated fields use source majority, shortest valid route and stable composite ID in that order, then fall back to the executor when no live field remains. Crossing a contiguous built Spiderlings web run costs two paid pull actions per cell and moves only on the final action. Stand firm costs `5 + 2 * (sources - 1)` displayed stamina and blocks the next legal owned pull. The owned leash adds `0.05 * (sources - 1)` to native Cut, Remove and Struggle escape penalty; this is the initial native-scaled candidate pending in-game balance calibration. External leash item data and native tether ownership remain unchanged. A Spinner cannot source player Recovery and NPC Capture at the same time; admission rejects either cross-over, and load auditing retains Recovery ownership when stale saved records overlap.

NPC Recovery uses the same source, executor, destination and crossing functions but owns no carrier or inventory event. `enemyMove` records an actual departure through a breached outer field before field entry handling. A later native Spinner melee must add Slime before one allowed owner attaches. The executor spends one operation and uses `KDMoveEntity(target, x, y, false)` for the single legal displacement; native and owned `pulled` guards prevent another movement that turn. Returning to a repaired common core ends recovery but does not create Capture strands. Combat attempts normal NPC Capture first on every later positive-Slime hit and clears Recovery only after admission succeeds.

The public runtime-boundary tests cover rule-only execution, shared hook composition and rendering without state writes. The complete watcher adds native-source compatibility tests. Final ZIP loading still requires the applicable in-game checks in the [verification matrix](../CONTRIBUTING.md#verification).

## Native Spinner line slice

The doorway scenario remains a focused #28 fixture. Ordinary and infestation maps use the same paid builder operations after Rollout snapshots the map decision and records an enclosure for each eligible saved group, without a global field or enclosure cap. If no legal site is available nearby, planning searches the connected map before waiting. It does not place an independent roadblock line on an ordinary map. Existing autonomous line plans retire their owned topology and reselect an enclosure; explicit authored line fixtures retain their behavior. Each handled construction pass performs one operation and rechecks terrain, protected metadata and actor occupancy.

The projection uses a non-immobile `SpiderlingsSpinnerTrap` enemy with `SpiderlingsWebTraversal` as its native path condition and the native `scenery` tag. KD 5.4.92 and the pinned reference apply swap admission after a successful path condition; the structural tag admits the Spiderling to the web cell. Ordinary enemies fail the condition and remain blocked. A Spiderling accepted by the condition enters the proxy cell without removing or swapping the web. One actual actor may stand on the web; another actor cannot enter that occupied cell. Reconciliation orders proxies before actors so native cached target lookup selects the actor when both share a cell. Direct web damage still requires the selected proxy identity. Projectiles and sight use the underlying floor tile. The adapter stores lines and enclosures in the same schema-2 physical graph and reconciles exactly one proxy per solid cell. Native enemy drawing selects `SpiderlingsSpinnerTrapTop`, `Side` and `Corner` pieces from the declared perimeter, with camera-aware placement and Normal/Pink selection. The old WebCell name exists only for native save hydration; load reconciliation replaces it while retaining entity identity and field durability. Independent ground traps, snare IDs and stepping-triggered slow are removed, including their old owned tile triggers and buffs.

Autonomous planning captures the map seed, forms groups from hostile capable Spinners within ten reachable steps and saves each selected plan before travel. A lone Spinner can select the smallest useful 3-by-3 outer enclosure. Candidate analysis reads floor topology, protected and locked metadata, entrances, exits, native choke hints, nests, actor occupancy and member origins. It checks exterior access before choosing a gate and records candidate, route and expansion work. No-site results wait for a geometry or relevant occupancy change before retrying. Rollout persists the AI's selected enclosure in `KDMapData.SpiderlingsSpinnerRollout` without replacing topology. Before detection, every capable member can hold one distinct work reservation, and native travel routes avoid live coworkers.

`afterDamageEnemy.dmgDealt` is the only durability input. Reconciliation then overwrites surviving proxy HP from topology and removes breached cells. Every solid-cell change sets `KDUpdateEnemyCache` and replaces both `KDPathCache` and `KDPathCacheIgnoreLocks`; `KDUpdateDoorNavMap()` alone is insufficient for the second cache in this patch. `afterLoadGame` reuses one valid proxy per cell, drops duplicates and creates missing projections without changing saved HP or ownership. Positive `tickAfter` events on the active map alone advance the final-owner collapse age.

Mobile actors tagged `spiderlings` or `spider` receive 1.5 times ordinary movement credit only when their live destination is an intact Spiderlings-owned web. Credit remains fractional across attempts; a blocked move cannot earn the extra credit, and leaving the web uses ordinary movement. No MoveSpeed buff is applied, so attack, spell, construction and wrapping budgets retain their own cadence. Spider pursuit uses a separate actor-aware weighted route with 2/3 base cost for owned-web steps, including visible-target direction choice. Web changes, actual actor moves and load clear its cache. Other actors and unrelated webs retain native routing.

## Native enclosing-field slice

Issue #92 uses the same map-owned topology and `SpiderlingsSpinnerTrap` projection as the doorway line. Every declared enclosure is a saved simple orthogonal polygon. The minimum outer footprint is 3 by 3 with one interior cell. One owner may build and close it. A completed body may gain a new outer ring one cell farther out, initially 5 by 5 and 7 by 7; every segment, closure and repair consumes a paid action. New autonomous rings leave aligned gates open until prey enters the common core. Existing auto-sealed fields retain their sealed-inner-ring expansion rules. Player Capture strands retain their separate two-source admission requirement and use only the inner interior cell for a minimum field.

The physical graph expands boundaries to unit edges, merges equal edges and coalesces collinear spans only while owner sets match. Original polygon vertices remain real anchors. Overlap splits and crossings are junctions without anchor HP. Identical and partial overlaps therefore share one HP pool per physical span and one native proxy per occupied cell.

Builders complete the inner body before adding an outer ring. Each autonomous ring retains one aligned entrance into the common core. A living hostile player or non-Spiderling NPC inside that core arms only that composite's closure; nearby actor movement does not cancel it. Closing each gate requires one paid cell operation and one paid connection operation. Once prey leaves or disappears, paid reopening restores the entrances, including partly closed gates. Completed body cells remain. Declared containment, outside-field reachability, outside-composite reachability and the route to the floor exit are separate queries. A crossing without a declared polygon is never a capture field.

Live owners may spend one action to restore 10 percent of a damaged structure's maximum HP. A destroyed anchor or link waits four positive active-map turns before paid rebuilding. Retired-only work is not repaired. The final valid owner still starts the 20-turn collapse count. `afterLoadGame` restores partly built, sealed and breached graphs and reconciles proxies idempotently.

The pinned 31-by-21 regular-room test keeps start `(2,10)`, exit `(28,10)`, worker starts `(11,9)` and `(12,11)`, boundary `(20,5)-(24,5)-(24,13)-(20,13)`, core `(22,9)` and gate `(20,9)`. Arrival-inclusive 2/4/8-worker results are 25/19/15 native world turns; legal-worksite starts take 15/6/4. The prototype references are 27/20/18 and 12/6/4. Native occupancy avoidance and movement to remaining work account for the differences. The test retains blocked and unfinished measurements instead of discarding them.

Debug scenarios use the same enclosure and controller runtime as Rollout. They admit Capture strands only after a qualifying native Spinner hit; field damage after admission does not cancel those strands. Scenario control saves selected actor IDs, target ID, awareness and the pre-existing encounter so a real load can resume controls and teardown restores unrelated state.

## Native lure-coordination slice

Issue #31 extends each saved group with one engagement. The target comes from the native enemy-loop argument after KD computes hostility and perception, so it may be the player or a hostile NPC. The first valid observation fixes the target and one lure. Later native target choices do not rotate that saved target. Disabled, helpless, dead, missing or no-longer-hostile role holders are audited before actions and replaced deterministically.

Only a qualifying observer copies a target cell into group state. The detection turn stores age zero. Positive `tickAfter` events advance the age once; ages zero through three are usable, and age four removes both the cell and direction. Eight completed turns without actual sight switch the lure to delegated native pursuit. A later valid observation restores lure mode. Hearing can refresh the last-known cell, but only native sight resets the no-sight counters.

The lure uses native paid movement to vacate pending gates and work cells, avoid adjacent contact when a legal choice exists and retreat toward the saved field waypoint. Other capable members retain #29 reservations and use #30's enclosure scheduler. On common-core entry or withdrawal, outstanding paid gate work temporarily includes the lure and takes priority over adjacent defense, so even a lone builder can close and reopen its entrance. Other adjacent, actually perceived hostile targets select native defense for one pass; the native attack accumulator decides whether a hit is ready. Every other handled lure, movement, work, yield or wait decision blocks the later native attack and spell phases for that actor and world turn.

`afterLoadGame` removes invalid members and targets, expires stale last-known data, replaces an invalid lure, clears transient decisions and deduplicates saved task and work-cell claims. It neither selects a new field nor creates a job. `setupCooperative()` and `spiderlingsSpinnerCooperative` enter this same production path for manual testing. Automated evidence and the remaining graphical test limit are recorded in [Spinner lure coordination evidence](spinner-lure-coordination.md).
